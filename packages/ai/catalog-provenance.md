# Built-in model catalog provenance

`src/models.generated.ts` is the checked-in runtime catalog. Normal builds do not fetch live feeds. This file records source qualifications and review decisions for the snapshot, not a second runtime authority or a promise that every model is available to every account.

## Snapshot reviewed 2026-09-23

The previous post-#568 snapshot had 32 providers / 1,062 records (SHA-256 `089e634c71fc3b6bbdbb6563114851aa500d633a0d71e9876aa0728a42dd33dc`). The corrected snapshot has **32 providers / 1,360 records** (SHA-256 `10d32d418a3049e945f5b4fb886999c1dea604fb818a8dc263ec7fae2d53ee6b`): compared with the previous committed snapshot, 460 additions, 162 removals, 263 changed shared records and 638 unchanged shared records. These counts compare provider/ID objects in the two committed catalogs; the earlier 482-changed/393-unchanged inventory did not match that comparison. The 26 retentions against live feed omission are a *separate candidate-versus-snapshot* classification, not another category of the committed baseline diff. The checked-in generated diff supplies exact old/new provider/ID/field values; the dispositions below explain the material groups.

Sources observed on 2026-09-23: live `https://models.dev/api.json`, `https://openrouter.ai/api/v1/models` with per-tool-model endpoint discovery, and `https://ai-gateway.vercel.sh/v1/models` with endpoint discovery. The isolated normalized review candidate had SHA-256 `d1a33269e9808e59c26d4d9678d452173b13f9dc4f005deccc1eaca469eea276` (31 providers / 1,366 records). The upstream Pi comparison used `earendil-works/pi` commit `7fd564cbb78f35f3de14d5382fea692b87ec4026` and published MIT `@earendil-works/pi-ai@0.87.1` data (tarball SHA-256 `35b4432f27cc2665f86beebb9af6a39b1251970883c3044bd8be4f4e8c731ca0`; manifest generated `2026-09-22T19:31:44Z`). **471 of 491 candidate additions** appeared in that Pi release. Live feeds change; the hashes identify observations, not reproducible historical feed inputs. No paid model calls were made.

The review standard is proportional to observed use: direct Anthropic, OpenAI and OpenAI Codex receive closer route-specific checks; other providers use validated live metadata and upstream Pi as corroboration, with concrete conflicts examined. Pi's different per-provider topology, protocol selection, pricing tiers and account routes are not automatically adopted. Missing source prices are not evidence of free usage: 1,284 missing scalar fields were estimated from Pi only when provider, ID, API and base URL matched exactly; 32 used an existing Scramjet same-route value. These are **usage estimates**, not billing truth. The 30 new records still lacking an applicable estimate were excluded rather than serialized with `null` or guessed zero. One more proposal, direct OpenAI `gpt-realtime-2.1`, was excluded because [OpenAI lists Realtime but not Responses](https://developers.openai.com/api/docs/models/gpt-realtime-2.1) for that ID, contrary to both catalog feeds. The review also removed the previously checked-in Azure Responses copy of that ID: Azure deployment-specific Responses support was not established, and the generator's Azure-copy path cannot reproduce it from the filtered direct source. This does not assert that Azure cannot deploy the model through another API.

**Retained despite omission (26):** All three existing `kimi-coding` IDs (`k2p7`, `kimi-for-coding`, `kimi-k2-thinking`) because its models.dev section disappeared; Azure `gpt-5-codex`, `gpt-5.1-codex-mini`, `gpt-5.2-codex` because direct-API retirement is not Azure retirement; Cloudflare `workers-ai/@cf/{moonshotai/kimi-k2.6,nvidia/nemotron-3-120b-a12b,zai-org/glm-4.7-flash}`; Fireworks `accounts/fireworks/models/{deepseek-v4-flash,glm-5p1}`; Zai `glm-5.1`; OpenRouter `kwaipilot/kat-coder-pro-v2`, `mistralai/devstral-2512`, `openrouter/auto`, `openrouter/free`, `thedrummer/unslopnemo-12b` and nine `~...-latest` aliases (Anthropic Fable/Haiku/Opus/Sonnet, Google Gemini Flash/Pro, Moonshot Kimi, OpenAI GPT Mini, xAI Grok). Sixteen remain in published Pi; the rest preserve existing/previously approved metadata until removal is substantiated. The legacy `openrouter/auto` negative sentinel cost is **not** a real negative provider price and remains an unresolved estimate for dynamic routing.

**Excluded for missing same-route cache prices (30):** Fireworks `accounts/fireworks/models/{glm-5p3,glm-5p3-flash,kimi-k3}` and `accounts/fireworks/routers/{glm-5p3-fast,glm-fast-latest,glm-flash-latest,glm-latest,kimi-k3-fast}`; Copilot and OpenCode `grok-4.7`; OpenRouter `aion-labs/{aion-3.5,aion-3.5-mini}`, `cohere/command-a-plus`, `qwen/qwen3.8-max-prime`, `stealth/space-bunny-alpha`, `upstage/solar-mini4`; xAI `grok-4.6`, `grok-4.7`; Xiaomi and each token-plan region (ams/cn/sgp) `mimo-v2.6-flash`, `mimo-v2.6-pro`, `mimo-v2.6-pro-ultraspeed`. The 161 feed/Pi-omitted built-ins (plus the separately excluded Azure Realtime copy) are not equivalent to 162 provider-confirmed retirements. Direct Anthropic Opus 4.1 IDs were [retired](https://platform.claude.com/docs/en/about-claude/model-deprecations) August 5, 2026; six removed direct OpenAI IDs were [shut down](https://developers.openai.com/api/docs/deprecations) July 23, 2026. Other catalog omissions do not prove that a separately configured deployment cannot serve the ID; external users may need a custom model definition.

**Material shared-record dispositions (committed baseline → corrected snapshot, reviewed 2026-09-23):** 263 changed records across the following route groups. Counts overlap across fields within a record. The source for feed-derived fields is the dated models.dev, OpenRouter endpoint/model or Vercel endpoint/model observation identified above; a field's changed value alone is not a provider guarantee. Where a field lacks independent provider confirmation, treat it as source-reported metadata or a scalar estimate, not an established billed rate or a paid maximum-request test.

| Route group | Changed records | Disposition and applicability |
| --- | ---: | --- |
| OpenRouter | 101 | Accepted 72 output allowances, 64 cost objects, 20 joint endpoint declarations and three total-context corrections from the OpenRouter **route's** dated model/endpoint metadata, not from the identically named direct providers. Endpoint tool support and joint limits remain request-specific; changed scalar prices are estimates. |
| Amazon Bedrock | 54 | Accepted 31 cost, 18 output, 11 total-context, two modality and three reasoning updates from the **Bedrock** models.dev section; names changed on ten records. The Bedrock Scout 10M/older 3.5M conflict remains explicitly unresolved below; neither a direct Meta model nor a historical Bedrock launch cap settles today's Bedrock limit. Regional Bedrock price variations and paid maximum acceptance were not tested. |
| Vercel AI Gateway | 46 | Accepted 33 cost, ten output, seven total-context, five joint-endpoint, three modality and one reasoning update from the **Vercel gateway's** dated model/endpoint responses. Endpoint declarations are not a universal guarantee across routed deployments. |
| Mistral | 28 | Accepted cache-read estimate changes from the Mistral models.dev route (previous cache-read values were zero); absence of a billing experiment means a nonzero scalar estimate is not a verified invoice. |
| Direct OpenAI / Azure | 5 / 5 | The direct five changed costs and Azure five copied-route costs use the dated OpenAI base-tier pricing qualification below; Azure deployment prices are not proven to equal public OpenAI billing. Azure Realtime's removal is separate from these shared-record changes. |
| Cloudflare AI Gateway / Workers AI | 6 / 1 | Accepted five gateway costs and three total contexts from its models.dev routes, and one Workers AI output update from its separate section. Underlying public-provider defaults do not prove Cloudflare deployment limits. |
| Fireworks / GitHub Copilot / Google / Hugging Face | 2 / 3 / 2 / 2 | Accepted route-scoped feed updates: Fireworks cost/output, Copilot scalar subscription estimates, Google costs and Hugging Face output. The authenticated Copilot account evidence described in user guidance applies to the named additions, not to all accounts or every scalar cost. |
| OpenCode Zen / OpenCode Go / Together / xAI | 2 / 4 / 1 / 1 | Zen's two API changes follow its provider-documented Responses route below; Go's four costs, Together's one and xAI's one are dated same-route feed estimates, not transferred API protocols or cross-route prices. |

The 24 changed total-context records belong to Bedrock (11), Vercel (7), OpenRouter (3) and Cloudflare AI Gateway (3). Route-relevant source observations and the explicit exceptions below govern them; no pricing threshold was substituted for a maximum, and no independent paid boundary request was performed. The 25 new or revised joint declarations belong only to OpenRouter (20) and Vercel (5). Name-only differences need no limit/pricing disposition. The source observations were not archived as byte-replayable historical payloads; a later feed response cannot prove the exact older observation.

**Removal/selection impact (baseline → corrected snapshot, reviewed 2026-09-23):** The 161 feed/Pi omissions break down by provider as Vercel (41), OpenRouter (34), Cloudflare AI Gateway (25), Copilot (10), Moonshot international and China (6 each), OpenAI (6), Fireworks (5), OpenCode (5), Together (5), Bedrock (4), Azure (3), Google (3), Anthropic (2), Cerebras (2), Groq (2) and Zai (2). The extra Azure Realtime removal is an unsupported **Responses mapping**, not proof of model retirement. Direct Anthropic (2) and direct OpenAI (6) have the provider shutdown evidence cited above; for other groups, the dated candidate plus matching published Pi absence justified removal from the *default catalog*, not a claim that every deployment or account has retired the ID. The 26 explicitly retained routes show that omission alone did not compel removal. Source qualifications vary by route: gateway and OpenRouter listings reflect their own endpoint catalogs; Copilot is subscription/account-scoped, Azure is deployment-scoped, Bedrock may be region-scoped, and direct, Moonshot and Cerebras accounts may differ. No external user's installed selections or privately configured deployments were inspected.

A repository check of removed provider/ID pairs against configured defaults, tests, examples and documented selection paths found one active default reference: `cerebras/zai-glm-4.7`. It is now changed to the surviving built-in `cerebras/gpt-oss-120b` with a catalog-existence test; the old default would otherwise be skipped during automatic provider selection. Saved provider/ID selections for **any** removed pair no longer resolve and fall back to another available model (a warning is printed where enabled). Scoped patterns without matches are skipped; `modelOverrides` do not recreate removed IDs. Users with a still-supported custom deployment must define an explicit `models.json` model with deployment-specific API, context, output and price metadata, then update saved selections/scopes; an override or an alias for an unrelated route is not a migration. A CLI unknown-ID fallback may construct a synthetic candidate but is not endpoint evidence. Repository reference checks cannot establish outside usage, and gateway/region/subscription availability remains unverified.

**Route and limit qualifications:** Direct Anthropic [model specifications](https://platform.claude.com/docs/en/models/overview) support 1M context / 128K synchronous output for Fable 5.1, Opus 5 and Opus 5.5. Direct Sonnet 4.5 remains 200K despite Pi's 1M entry because its long-context beta retired. Public OpenAI GPT-5.4/5.5/5.6/6 retain the applicable 1.05M maximum; Pi's 272K is a *pricing threshold*, not a context maximum. Codex retains its distinct 872K subscription route and estimates. Current [OpenAI scalar base-tier prices](https://developers.openai.com/api/docs/pricing) replace older GPT-5.6 Sol/Terra/Luna estimates; above 272K input, whole-request tiered charges can exceed scalar estimates. The [current AWS Bedrock Llama 4 Scout card](https://docs.aws.amazon.com/bedrock/latest/userguide/model-card-meta-llama-4-scout-17b-instruct.html) states 10M context, whereas the [2025 launch post](https://aws.amazon.com/blogs/aws/llama-4-models-from-meta-now-available-in-amazon-bedrock-serverless) stated a temporary 3.5M Bedrock cap; no paid boundary request resolved that historical discrepancy. OpenCode Go Qwen 3.7 uses the [provider-documented Messages route](https://opencode.ai/docs/go) despite the feed/Pi Completions mapping; Zen Grok uses the [provider-documented Responses route](https://opencode.ai/docs/zen). Do not transfer Pi API protocols between Copilot, Fireworks, OpenRouter or Xiaomi routes merely because the same model ID appears there.

## Future refreshes

Run the generator with `SCRAMJET_MODEL_CANDIDATE=<absolute-new-path>` in an isolated checkout to produce a non-runtime JSON inventory. Missing prices are `null` and listed in `unresolvedCosts`; unqualified dynamic routes/endpoints and absent previously supported optional models.dev sections (including `kimi-for-coding` → `kimi-coding`) are listed in `unresolvedSources`. The candidate is never the canonical TypeScript catalog. Normal `npm run generate-models` remains strict and refuses missing/malformed prices or acquisition failures before its canonical write; normal builds never run it. Compare the candidate with the checked-in snapshot and current, route-applicable sources before accepting a new snapshot. Runtime `Model.cost` still requires finite numeric estimates; zero is not a verified-free label. Offline tests check catalog structure and request metadata, not entitlements, provider charges or maximum-request acceptance.
